# Desmos graphing calculator API key (free, from https://www.desmos.com/my-api).
# Without it, the in-app calculator shows a "not configured" message instead of failing silently.
VITE_DESMOS_API_KEY=
